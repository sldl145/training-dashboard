{
  "name": "training-dashboard",
  "private": true,
  "description": "Pawel's training dashboard - validation tooling (the dashboard itself is index.html, no build step)",
  "scripts": {
    "validate": "node scripts/validate.js",
    "smoke": "node scripts/smoke.js",
    "check": "node scripts/validate.js && node scripts/smoke.js",
    "preflight": "bash scripts/hevy-preflight.sh; H=$?; echo; bash scripts/withings-preflight.sh; W=$?; exit $(( H > W ? H : W ))"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1"
  }
}
